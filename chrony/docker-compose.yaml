version: '3.9'

services:
  ntp:
    build: .
    image: cturra/ntp:latest
    container_name: ntp
    restart: always
    ports:
      - 123:123/udp
    environment:
      - NTP_SERVERS=time.apple.com
      - LOG_LEVEL=0
      - TZ=Europe/Berlin
#      - NOCLIENTLOG=true
#      - ENABLE_NTS=true
