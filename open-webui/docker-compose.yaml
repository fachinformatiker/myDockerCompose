services:
    open-webui:
        ports:
            - 3050:8080
        environment:
            - OLLAMA_BASE_URL=http://192.168.0.69:11434
        volumes:
            - open-webui:/app/backend/data
        container_name: open-webui
        restart: always
        image: ghcr.io/open-webui/open-webui:main
volumes:
    open-webui:
